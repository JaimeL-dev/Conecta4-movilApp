<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#BBDEFB"
    android:padding="20dp"
    tools:context=".activity_records2">

    <TextView
        android:id="@+id/tv_titulo_records"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="RÉCORDS Y ESTADÍSTICAS"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#1A237E"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp"/>

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tv_titulo_records"
        app:layout_constraintBottom_toTopOf="@id/btn_volver_inicio_records"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp"
        android:scrollbars="none">

        <!-- ESTE CONTENEDOR ES LLENADO DINÁMICAMENTE POR activity_records2.kt -->
        <LinearLayout
            android:id="@+id/ll_lista_records"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Las CardViews de ejemplo se han ELIMINADO.
                 Si el archivo JSON está vacío, se mostrará un mensaje.
                 Si hay récords, se generarán CardViews aquí. -->

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btn_volver_inicio_records"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Volver a inicio"
        android:textSize="18sp"
        android:paddingVertical="14dp"
        android:textColor="#1A237E"
        android:backgroundTint="#FFB300"
        app:cornerRadius="18dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>